# Mouse-Position-Calculator
Made a mouse position calculator using HTML 5, CSS 3 and Javascript, using "mousemove" argument in function on move . 
HTML 5: links,meta, div,heading,classes.
CSS 3:flex, alignment, border,position, transform, @media query
Javascript: fetch and add by eventlistner's function as an argument in which we add whole string in element. 
